export const DEFAULTS=Object.freeze({quality:'balanced',melanin:.18,roughness:.57,scattering:.55,detail:.8,iris:3,pupil:.34,hairMelanin:.25,hairRoughness:.62,hairLength:.85,breeze:0,exposure:.93,lightAngle:-36,lightPower:1.75,jaw:1,nose:1,smile:.08,blink:.02,brow:0,gazeX:0,gazeY:0,hair:true,animation:false,view:0,orbitYaw:.10,orbitPitch:.025,distance:6.4,targetY:.12});
export const RANGES={melanin:[0,1],roughness:[.24,.7],scattering:[0,1],detail:[0,2],iris:[0,3],pupil:[.22,.52],hairMelanin:[0,1],hairRoughness:[0,1],hairLength:[.4,1.8],breeze:[0,1],exposure:[.4,1.7],lightAngle:[-110,110],lightPower:[.4,3],jaw:[.7,1.3],nose:[.75,1.25],smile:[0,1],blink:[0,.98],brow:[-1,1],gazeX:[-1,1],gazeY:[-1,1],view:[0,5],orbitYaw:[-Math.PI,Math.PI],orbitPitch:[-.9,.9],distance:[2.3,12],targetY:[-1.5,1.5]};
export function validateConfig(input){if(!input||typeof input!=='object'||Array.isArray(input))throw Error('Expected a portrait settings object.');const out={...DEFAULTS};for(const [key,value]of Object.entries(input)){if(!Object.hasOwn(DEFAULTS,key))continue;if(key==='quality'){if(!['preview','balanced','high','stress','extreme'].includes(value))throw Error('Unknown quality preset.');out[key]=value;}else if(typeof DEFAULTS[key]==='boolean'){if(typeof value!=='boolean')throw Error(key+' must be true or false.');out[key]=value;}else{if(!Number.isFinite(value))throw Error(key+' must be finite.');const [min,max]=RANGES[key];if(value<min||value>max)throw Error(`${key} must be between ${min} and ${max}.`);if(['iris','view'].includes(key)&&!Number.isInteger(value))throw Error(key+' must be an integer.');out[key]=value;}}return out;}
export function geometryLayout(quality='balanced'){
 const tier={preview:{head:[160,140],eye:[64,32],lid:[80,24],hair:4500,segments:8,brows:450},balanced:{head:[240,200],eye:[96,40],lid:[112,32],hair:12000,segments:10,brows:750},high:{head:[320,256],eye:[144,64],lid:[160,40],hair:22000,segments:12,brows:1100},stress:{head:[384,320],eye:[160,80],lid:[192,48],hair:50000,segments:12,brows:1500},extreme:{head:[384,320],eye:[160,80],lid:[192,48],hair:100000,segments:12,brows:1500}}[quality];if(!tier)throw Error('Unknown geometry preset.');
 const layout=[];let first=0;
 const add=(kind,nu,nv,fiber=false)=>{const count=nu*nv*6;layout.push({kind,nu,nv,first,count,fiber});first+=count;};
 add(0,...tier.head);add(1,...tier.eye);add(2,...tier.eye);add(3,...tier.lid);add(4,...tier.lid);add(5,112,24);add(6,112,24);add(7,112,3);add(8,96,28);add(9,96,28);add(10,112,40);add(11,112,48);add(12,144,64);add(18,48,12);add(19,48,12);
 const skinVertices=first;add(13,tier.hair,tier.segments,true);add(14,tier.brows,4,true);add(15,tier.brows,4,true);add(16,144,5,true);add(17,144,5,true);
 return {layout,vertices:first,triangles:first/3,strands:tier.hair+2*tier.brows+288,scalpStrands:tier.hair,skinVertices};
}

